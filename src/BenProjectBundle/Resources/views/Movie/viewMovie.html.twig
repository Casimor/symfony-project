{% extends "::base.html.twig" %}

{% block body %}
<div class="panel panel-success">
	<div class="panel-heading">
		<h1>{{ title }}</h1>
	</div>
	<div class="panel-body">
		Date de sortie: {{ releaseDate }}<br>
		Synopsis: {{ synopsis }}<br>
		Réalisateur: {{ director }}
	</div>
    <a href="{{ path('modify_movie', {'id': movie.id}) }}" class="btn btn-danger">Modifier</a>
    <a href="{{ path('delete_movie', {'id': movie.id}) }}" class="btn btn-danger">Supprimer</a>

</div>
{% endblock %}